# Attempt 05 - Hero Premium 3D

Snapshot after introducing the premium Three.js hero direction.

- Moves the homepage opening toward the attorney portrait and "Защитата започва преди делото."
- Adds `HeroScene3D.jsx` with animated full-bleed Three.js canvas.
- Tightens mobile first-screen layout so the portrait appears earlier.
- Updates hero signal cards to dark forest-glass for stable contrast.
- Keeps the intro loader but shows it once per session.
- Includes package files because this attempt adds the `three` dependency.
